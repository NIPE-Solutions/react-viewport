export function InteractionHint() {
  return (
    <aside className="interaction-hint" aria-label="Try the live examples">
      <strong>Try it live:</strong> pinch zoom in/out (where supported), resize, scroll or open the
      keyboard. Watch the geometry and examples respond.
    </aside>
  )
}
